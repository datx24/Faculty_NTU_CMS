# Faculty_NTU_CMS
 Đồ án phát triển ứng dụng web 2

hướng dẫn db migration:

- đối với chạy lần đầu:
    1. setup mysql và tạo db : create database facultyntucms, use facultyntucms
    2. nạp file sql theo thứ tự: schema.sql, tschema_update_1_may.sql
- đối với anh Đạt (đã có db trong máy, chạy script update db): 
    git pull về và chỉ chạy file tschema_update_1_may.sql vào mysql ( USE facultyntucms)

ps: với cả để mật khẩu cho mysql đi , 123 cũng được


